<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sean.assignment.mapper.cultureMapper">
    <select id="list" resultType="com.sean.assignment.pojo.Culture">
        Select *
        from cultures
        <where>
            <if test="email != null">
                email = #{email}
            </if>
            <if test="name != null">
                AND name LIKE CONCAT('%',#{name},'%')
            </if>
        </where>
    </select>
    <select id="hasCulture" parameterType="com.sean.assignment.pojo.Culture" resultType="boolean">
        SELECT CASE WHEN COUNT(*) > 0 THEN TRUE ELSE FALSE END
        FROM cultures where name = #{name}  AND email = #{email}
    </select>

    <update id="update" parameterType="com.sean.assignment.pojo.Culture">
        update cultures
        set email = #{email},
        name = #{name},
        address = #{address},
        <if test="img != null">
            img = #{img},
        </if>
        description = #{description}
        where email = #{email} and name = #{name}
    </update>
</mapper>