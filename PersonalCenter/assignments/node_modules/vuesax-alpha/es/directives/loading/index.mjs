import { isRef } from 'vue';
import '../../utils/index.mjs';
import { isBoolean } from '../../utils/types.mjs';

const loading = {
  mounted(el, binding) {
    const value = binding.value;
    if (isBoolean(value) || isRef(value)) {
      return;
    }
  }
};

export { loading as default };
//# sourceMappingURL=index.mjs.map
